dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Play Billing Codelab"
include ':start', ':finished'

sourceControl {
    gitRepository("https://github.com/revenuecat/purchases-android") {
        producesModule("com.revenuecat.purchases:purchases")
    }
}